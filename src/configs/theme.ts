import { Theme } from '../typescript/type';

const light: Theme = {
  background: '#fff',
  accent: '#000',
  primary: '#55f244',
};

const dark: Theme = {
  background: '#000',
  accent: '#fff',
  primary: '#55f244',
};

const colors = {
  light,
  dark,
};

export default colors;
