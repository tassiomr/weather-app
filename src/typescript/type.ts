export type Theme = {
  background: string;
  accent: string;
  primary: string;
}

export type Sizes = {
  icons: {
    large: number,
    medium: number,
    small: number,
   },
   font: {
    header: string,
    subtitle: string,
    paragraph: string,
   },
}